<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="FormStyle">
        <item name="heading">@style/headingStyle</item>
        <item name="formText">@style/headingStyle</item>
    </style>

    <style name="DefaultPadding">
        <item name="android:minHeight">@dimen/all_min_height</item>
        <item name="android:paddingLeft">@dimen/form_padding_horizontal</item>
        <item name="android:paddingRight">@dimen/form_padding_horizontal</item>
        <item name="android:paddingTop">@dimen/form_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/form_padding_vertical</item>
    </style>

    <style name="headingStyle" parent="DefaultPadding">
        <item name="android:background">@color/colorBackgroundPrimary</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">20sp</item>

    </style>

    <style name="formText" parent="DefaultPadding">
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
